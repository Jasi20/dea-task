let evenNums = [];
const randomNums = Math.floor(Math.random() * 100 + 1 )

if(randomNums %2 === 0) {
        evenNums.push(randomNums)
        console.log(`random eded cutdur: ${randomNums}`);
        console.log(evenNums);
    }
    else {
        console.log(`random eded tekdir: ${randomNums}`);
    }



